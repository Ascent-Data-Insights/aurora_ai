[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_BASE = "https://aurora-api.ascentdi.com"
  VITE_WS_BASE = "wss://aurora-api.ascentdi.com"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
