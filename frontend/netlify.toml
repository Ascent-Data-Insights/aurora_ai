[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_BASE = "https://portfolio-agent-api.ascentdi.com"
  VITE_WS_BASE = "wss://portfolio-agent-api.ascentdi.com"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
